module.exports = (app) ->
  # app.resource 'users', app.controllers.user
  # app.get '/', (req, res) -> res.send req.user
  # app.io.route 'ready', (req) ->
  #   console.log 'ready', req.data
  #   req.io.join req.data
  #   console.log req.io.room req.data
  #   (req.io.room req.data).broadcast 'announce', {message: 'New client in the ' + req.data + ' room. '}
